proc int compute(int x) {

    int i;
    int total = 0;
    loop(i = 0; i < 10; i++) {
        total = total + i;
    }

    if (total > 5) {
        dec a = 10.26;
    } else {
        out("YAY!");
    }

    ret x * x;
}