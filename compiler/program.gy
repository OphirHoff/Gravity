proc int compute(int x, char ch, dec f) {

    int i;
    int total = 0;
    loop(i = 0; i < 10; i++) {
        total = total + i;
    }

    if (total > 5) {
        dec a = 10.26;
    } else {
        out("YAY!");
    }

    ret x * x;
}

proc int main() {

    int a = 5;
    char b = 's';
    out("hi");
    $compute::(a, b, 89.954);

}